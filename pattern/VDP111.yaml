type: Variable Design Pattern
subtype: Property Pattern
id: https://github.com/mabablue/I-ADOPT-patterns-playground/blob/main/pattern/VDP110.yaml
name: property (cover)
definition: Cover expresses the fraction of a total reference area that is occupied by the Object of Interest. 
design: Asymmetric system (numerator/denominator) at the object of interest slot. The portion is represented by the numerator, the total reference area by the denominator.
ObjectOfInterest: Asymmetric System (Num/Denom)
Matrix: null
required_constraint: null
constrained_component: null
associated_patterns:
  - https://github.com/mabablue/I-ADOPT-patterns-playground/blob/main/pattern/VDP35.yaml
associated_mapping: []
path: "DT0:Units(F) -> Unitless(Quantitative) -> Quantitative(ratio) -> DT1:SameQuantity(T) -> Exeed1(F) -> Cover(T) => VDP111"
alt_path: "DT0:Units(T) -> DT2:Length(F) -> OtherFund(F) ->DT3:Intrinsic(F) -> Atomic(F) -> Ratio(T) -> Time(F) -> DiffQuantities(F) -> DiffEntities(T) -> Cover(T) => VDP111"
example: cloud cover
gh_issue: https://github.com/mabablue/I-ADOPT-examples-playground/issues/9
