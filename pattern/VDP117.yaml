type: Variable Design Pattern
subtype: Property Pattern
id: https://github.com/mabablue/I-ADOPT-patterns-playground/blob/main/pattern/VDP117.yaml
name: direction
definition: A spatial or angular measurement indicating the orientation, heading, or trajectory of an entity relative to a specified reference frame (vector).
design: OoI with two parts. One part must be the reference vector.
ObjectOfInterest: Symmetric System (part)
Matrix: null
required_constraint: teference
constrained_component: property
associated_patterns:
  - https://github.com/mabablue/I-ADOPT-patterns-playground/blob/main/pattern/VDP33.yaml
associated_mapping: []
path: "DT0:Units(T) -> DT2:Length(F) -> OtherFund(F) ->DT3:Intrinsic(F) -> Atomic(F) -> Ratio(T) -> Time(F) -> DiffQuantities(F) -> DiffEntities(F) -> OneRef(F) => VDP117"
alt_path: null
example: direction of radial velocity
gh_issue: https://github.com/mabablue/I-ADOPT-examples-playground/issues/62
