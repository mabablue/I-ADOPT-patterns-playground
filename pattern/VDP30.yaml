type: Variable Design Pattern
subtype: Property Pattern
id: https://github.com/mabablue/I-ADOPT-patterns-playground/blob/main/pattern/VDP30.yaml
name: index
definition:  An index acts as a proxy property that normalizes data against a specific base point or reference value to provide relative context rather than absolute measurement. While it sometimes aggregates multiple variables into a single score, its primary function is to make diverse data sets comparable over time or across different subjects.
ObjectOfInterest: Single
Matrix: ~
required_constraint: ~
constrained_component: ~
associated_patterns: 
  - https://github.com/mabablue/I-ADOPT-patterns-playground/blob/main/pattern/VDP33.yaml
associated_mapping: []
path: "DT0:Units(T) -> DT2:Length(F) -> OtherFund(F) -> DT3:Ratio(T) -> Time(F) -> DifferentQuantities(F) -> DiffEntities(F) => VDP03"
alt_path: ~
example: BMI
gh_issue: https://github.com/mabablue/I-ADOPT-examples-playground/issues/107
